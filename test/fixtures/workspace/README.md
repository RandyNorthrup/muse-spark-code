# Integration test workspace

Opened as the workspace folder by `@vscode/test-cli` (see `.vscode-test.mjs`).
Kept deliberately empty apart from this file and `.vscode/settings.json`,
which sets two machine-scoped settings (must be ignored) and one
window-scoped setting (must apply) for the workspace-trust tests.
