![The empty panel with its getting-started tips](open.png)

**Click the Muse Spark icon in the activity bar.**

`Ctrl+Shift+Escape` (`Cmd+Shift+Escape` on macOS) opens a conversation in an editor tab instead. Each tab is its own conversation and comes back after a window reload. `Ctrl+Escape` moves the keyboard focus between the editor and the composer.
