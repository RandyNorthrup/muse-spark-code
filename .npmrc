save-exact=true
engine-strict=true
fund=false
# Ignore package versions published fewer than this many days ago (supply-chain
# hygiene: gives the registry time to yank a compromised release). Exact pins in
# package-lock.json are unaffected; this only shapes new resolutions.
min-release-age=7
