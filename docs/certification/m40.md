# M40 certification — the panel in VS Code's display languages, part a (PLAN.md M40, D33)

Recorded 2026-09-25.

The owner said "yes" (2026-09-24) to the fourteen languages VS Code ships,
with machine translations disclosed. M40a is the machinery, the English
table and the gate. The panel still reads in English; M40b adds the
translations.

## How the work was split

The lead wrote the shared part first:

- the table (`src/shared/l10n/en.ts`) with templates, plural forms and label
  groups;
- the helpers (`text.ts`: `fill`, `plural`, `templateParts` and the `Intl`
  formatting);
- the checks (`check.ts`) and the language list (`locales.ts`);
- `MODEL_TEXT`, the English kept for the model.

Three agents then worked in parallel on separate files, each adding keys only
in its own marked section of `en.ts`:

- **host and core:** the model text, the sentence templates, the loader
  (`src/host/l10n.ts`) and the table in the webview HTML;
- **webview and shared UI:** templates, plural forms, `Intl`, and
  `installTable.ts`;
- **tooling:** `package.nls.json`, `scripts/check-l10n.mjs`, the harness's
  `--lang` and the pseudo-locale.

The lead then:

- removed the 17 keys no code reads any more;
- turned the sentences that ended in a value into templates;
- moved the last English out of constants.ts: status verbs, onboarding tips,
  dictation reasons, the sign-in terminal's name and Muse Code's exit
  meanings;
- localized dollar amounts;
- fixed the "1000K" edge.

## Drills

Each was broken once on purpose, seen to fail, and restored.

**The checks and the lead's changes**

| Drill                                                  | Result                                                                                 |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| L1: `check.ts` stops reporting a value left in English | `l10n.test.ts` fails 1 test: the expected `modes.manual: left in English` is not there |

**The gate** (`scripts/check-l10n.mjs`). G1 to G17 ran on a scratch copy
listing `de` and `ru` with valid tables, whose baseline exits 0; each break
exits 1 with exactly this message:

| Drill                                        | Message                                                                                                                   |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| G1: a key deleted                            | `l10n/ui.de.json: crashReload: missing`                                                                                   |
| G2: an extra key                             | `bogusKey: not in the English table`                                                                                      |
| G3: a slot renamed                           | `thoughtFor: slots {duration} expected, found {time}`                                                                     |
| G4: Russian plural forms with only one/other | `agentsCount: ru uses the forms few, many, one, other; found one, other`                                                  |
| G5: a value left in English                  | `crashTitle: left in English`                                                                                             |
| G6: the backticks dropped                    | ``signInBrowserDetail: the code spans (`) differ from the English``                                                       |
| G7: `**` added                               | `crashReload: the bold markers (**) differ from the English`                                                              |
| G8: a table for an unlisted language         | `l10n/ui.fr.json: fr is not in TABLE_LOCALES`                                                                             |
| G9: a listed table missing                   | `l10n/ui.ru.json: missing (TABLE_LOCALES lists ru)`                                                                       |
| G10: broken JSON                             | `l10n/ui.de.json: Unexpected end of JSON input`                                                                           |
| G11: an unknown key in the allowlist         | `l10n/untranslated.json: ui.*: noSuchKey: not a string of the ui table`                                                   |
| G12: a key listed twice                      | `ui.*: exportItem: listed twice`                                                                                          |
| G13: a manifest translation broken           | `package.nls.de.json: command.signOut.title: left in English` and `command.hooks.title: missing`                          |
| G14: a manifest translation for no table     | `package.nls.fr.json: fr is not in TABLE_LOCALES`                                                                         |
| G15: a plain manifest title                  | `package.json.contributes.commands[0].title: "Open in Sidebar" is shown to the user; make it a %key% of package.nls.json` |
| G16: a key removed from `package.nls.json`   | `%command.showLogs.title% is not in package.nls.json`                                                                     |
| G17: an unused `package.nls.json` key        | `command.unused.title: not used by package.json`                                                                          |
| G18: a key vsce would refuse                 | `%command.open-in-new-tab.title%: vsce accepts only letters, digits, _ and . in a key`                                    |
| G19: `UI_TEXT` read at module load           | 11 planted reads in a scratch module, all reported (`… UI_TEXT read at module load (a class field initializer) …`)        |

In G19, eight uses that do not run at load were left alone: a method, a
getter, an arrow field, a parameter default, an object key, a nested arrow,
a type and a re-export. On the real files, restored byte for byte after each
run:

- a key removed from `package.nls.json` failed with `%command.hooks.title% is
not in package.nls.json`;
- a stray `l10n/ui.de.json` failed with `de is not in TABLE_LOCALES`;
- a bad allowlist entry failed with `ui.de: not a language TABLE_LOCALES
lists`.

While the agents were mid-edit, the gate caught two real module-load reads,
`App.tsx`'s attach menu and `Transcript.tsx`'s rewind menu. Both became
functions.

**The loader, the HTML and the webview**

| Drill                                                                  | Result                           |
| ---------------------------------------------------------------------- | -------------------------------- |
| H1: the table's JSON embedded in the webview HTML without escaping `<` | `html.test.ts` fails (exit 1)    |
| H2: the host loader skips the shape check                              | `hostL10n.test.ts` fails 2 tests |
| W1: `installEmbeddedTable` never calls `setUiText`                     | `installTable.test.ts` fails 1   |
| W2: its table check inverted                                           | `installTable.test.ts` fails 2   |
| W3: History's group titles read into a module-level constant again     | `sessions.test.ts` fails         |

**The harness in a table**

- A pseudo table with a key removed fails the page with the webview's own
  refusal message.
- `?lang=xx` fails with `HTTP 404`.
- `?lang=../../package` fails with `not a VS Code language id`.

## Seen

`npm run harness:shots -- --lang=pseudo` rendered the `empty`, `tools`,
`approval`, `usage`, `agents`, `modes`, `palette`, `history`,
`slash-commands`, `question` and `transcript` scenarios. The screenshots
were viewed.

- **Bracketed, so translated:** every text the extension owns: labels,
  hints, the onboarding tips, the Modes menu, the usage dialog, the approval
  card with its code subject where the template puts it, and plural
  counts.
- **Unbracketed, as expected:**
  - the product name;
  - the names allowlisted in `l10n/untranslated.json`;
  - the fixture conversation;
  - the approval choices, which are Muse Code's own labels.
- **One gap found:** the Agent map showed a background task's raw status
  (`inProgress`, `completed`). Statuses are now the `agentStatuses` group,
  and one the table does not list is shown as it came (a unit test covers
  both).

Long pseudo strings, about a third longer than English, wrapped without
overlap in the tips, the palette, the Modes menu and the usage dialog.

## The accessibility gate in the pseudo-locale

`node scripts/a11y.mjs --lang=pseudo` ran every scenario in the four themes
(216 pages). It found two real problems that English runs had not reached.

- **A dangling `aria-controls`** (WCAG 4.1.2), in `filter` in every theme.
  The `filter` scenario types "eff". That matches an action in English but
  nothing in the pseudo-locale, and the palette's search box kept
  `aria-controls="palette-listbox"` after the list was gone. History's search
  box had the same flaw.
  - The fix: both boxes control a list, and say they are expanded, only
    while one is shown. Unit tests check a search that matches nothing.
  - Drill: `aria-controls` made unconditional again fails
    `Palette.test.tsx` (1 test); restored.
- **A crowded target** (WCAG 2.5.8), in `jump`, one theme, one run. A code
  block's buttons are 17 px tall, and English passes on spacing alone;
  longer labels squeezed that spacing below 24 px.
  - The fix: the buttons are 24 px targets. The `jump` screenshot was
    viewed: the bar is a little taller, with nothing else changed.

After both fixes, the affected scenarios pass in the pseudo-locale in every
theme (`jump`, `tools`, `rewind`, `usage`, `dictation`, `filter`,
`history`: 28 pages, 0 violations).

**Flakes, reported as seen:** two runs had pages without a result.

- One full pseudo run: `usage`, `dictation` and `rewind` in the dark theme.
  A scenario step ran before the element it clicks had rendered.
- One English run of `jump` and `tools`: 4 pages.

Neither came back in the reruns: the next full pseudo run, and three English
runs of those two scenarios. Every full English run of the gate for this
milestone has shown 0 pages without a result.

## What a user sees change in English

- History's times: "5 min. ago", "3 hr. ago", "yesterday", then a date.
- The usage window's reset: "2h 5m". Durations: "45s", "1m 30s".
- An exit code reads "exit code 1".
- The hooks view reads "3 hooks in your settings".
- A token count just under a million reads "1M".
- The approval card's name for screen readers is its whole sentence.

## The gate

`npm run quality` on the finished branch:

- **Style and structure:** format, lint (PSScriptAnalyzer 0 findings), the
  five type projects, knip and cycles are clean; `Found 0 clones.`
- **Localization:** `l10n: 0 tables, 76 manifest strings, 165 source files;
0 problems`.
- **Tests:** 131 files passed and 1 skipped; 1349 tests passed and 3 skipped.
  Coverage: statements 96.86 %, branches 91.92 %, functions 97.23 %, lines
  96.83 %.
- **Build:** `extension.js` 318.0 KiB, `searchWorker.js` 35.6 KiB and
  `main.js` 661.2 KiB, all within budget. The webview grew 7.4 KiB and the
  translations stay out of it.
- **Security:** audit 0 advisories; gitleaks found no leaks.
- **Accessibility:** `a11y: 216 pages (54 scenarios × 4 themes), 0 rules
violated on 0 elements, 0 rules undecided on 0 elements, 8 exempt, 0 pages
without a result`.
- **Semgrep:** not on the Bash tool's PATH, so `security:sast` failed there;
  from PowerShell it reported `Ran 255 rules on 288 files: 0 findings.`
