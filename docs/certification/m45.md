# M45 certification — session goals (PLAN.md M45, D38)

Recorded 2026-09-25.

Before M45 the panel showed the goal tools' rows (M43) and nothing else:
Muse Code's `goal/*` verbs, its `session/goalChanged` and a resumed
session's goal never reached the panel, and the Model API backend had no
goals at all.

## The live capture

The goal surface is built from what Muse Code 1.3.0 actually sends
(AGENTS.md rule 13), captured on 2026-09-25 with the owner's standing
go-ahead for live checks (no ask, contributor model, empty workspace,
count afterwards):

- **Where:** two new, empty folders, `C:\muse-live-m45` and
  `C:\muse-live-m45-stop`.
- **How:** `muse serve --trust-workspace` driven over MSP by capture
  scripts, approval mode `allowAll`, every notification, ack and error
  written to a JSON lines file. Model `muse-spark-1.3-contributor`.
- **What, in the first folder:** each verb on a session with no goal (no
  model call); an ordinary one-word turn with `goal/set` and `goal/pause`
  sent while it ran; `goal/edit` of the paused goal while idle;
  `session/resume` with `inline` and with `snapshot` history, and
  `session/read`; `goal/resume` while idle (the goal-driving turn, which
  closed the goal with `update_goal`); pause, resume and edit of the
  finished goal; `session/resume` with a snapshot again; `goal/clear`
  twice.
- **What, in the second folder:** `goal/set` while idle, then `turn/cancel`
  (the panel's Stop) of the turn it woke, then `goal/clear`.
- **Cost:** three turns (one ordinary, two goal-driving), **nine model
  attempts**, all on the contributor model, counted from the CLI's trace
  logs (`event="model.attempt.lifecycle" phase="admission"`): seven in
  `cli-3ad5bc45-….log`, two in `cli-f268bbdd-….log`, each identified by
  the capture's own session and run ids.

What the capture showed, and what the extension now does with it:

| Muse Code sends                                                                                                                                                          | The extension                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| `goal/set` idle: `{ commandId, status: "accepted", turnId }`, `turnId` naming the woken turn (equal to the command id); the turn has **no** `userMessage` item           | The turn arrives as its own `turn/started`; the transcript has no card for it (the notice "Goal set: …" marks it)  |
| `goal/set` or `goal/resume` while a turn runs: the ack names that turn; nothing starts                                                                                   | Nothing more to do; the controller logs the turn named                                                             |
| `goal/pause`, `goal/clear`: the ack names no turn                                                                                                                        | The same                                                                                                           |
| `goal/edit` of a paused goal while idle: accepted, the goal stays paused, nothing wakes                                                                                  | The Model API backend follows the same rule                                                                        |
| `commandRejected` (-32030) with `data.reason` `missing_goal` (pause, resume, edit, clear with no goal) or `invalid_goal_state` (pause, resume, edit of a finished goal)  | `GoalRefusedError` `noGoal` / `wrongState`; a warning in words ("There is no goal…", "Only a paused goal can be…") |
| A blank objective: `invalidParams` "goal/set objective must be non-empty"; an objective on a bare verb: `invalidParams` "unknown field `objective`"                      | Refused before sending; a bare verb never carries an objective                                                     |
| `session/goalChanged` `{ goal: { objective, status, percentComplete, currentWork?, nextWork? } }`; `goal: null` on clear                                                 | `goalChanged` in the agent events; the goal strip                                                                  |
| No `session/goalChanged` after a resume; inline history has no goal; a snapshot has `state.goal` (`null` when none), and the task list                                   | A resume asks for `history: "snapshot"`; the snapshot's goal and task list reach the panel                         |
| `turn/cancel` of a goal turn: `goalChanged` with `status: "paused"`, then `turn/completed` `cancelled`                                                                   | Stop pauses the goal on Muse Code by itself; the Model API backend pauses it too                                   |
| The goal tools' results (M43's capture): `{ goal: { session_id, goal_id, objective, status, percent_complete, current_work, next_work, token_budget, tokens_used, … } }` | The Model API backend's four goal tools answer in exactly this shape                                               |

The goal store's status list (active, paused, complete, blocked,
usage_limited, budget_limited), the goal tools' descriptions, `update_goal`'s
"expected complete or blocked", `report_progress`'s 0–100 and non-empty
work, and `create_goal`'s refusals over an unfinished or paused goal were
read from the strings of the Muse Code 1.3.0 binary; the step probe's note
("Progress has not been reported in the last N model calls…") and the goal
turn's cue ("Continue working toward the active session goal.") too.

## What was built

- **Muse Code backend**: `session/goalChanged` mapped (a `null` or absent
  goal clears; `status` and `percentComplete` verbatim; a malformed goal is
  dropped with a log line); `controlGoal` sends `goal/<verb>` with the
  objective only on `set` and `edit`, and turns the two captured refusals
  into `GoalRefusedError`; a resume asks for the snapshot, whose goal is
  read on its own so a goal of another shape never costs the resume.
- **Model API backend** (`src/core/backends/modelapi/goals.ts`): the four
  goal tools with Muse Code's arguments, rules, messages and result shape;
  the goal stored with the session (optional, so older files still read)
  and forked with it; the goal pinned as the last section of the
  instructions while active; the step-probe note after ten model calls
  without progress; tokens counted against a budget (`budget_limited` when
  spent); Stop pausing an active goal; the user's verbs with MSP's rules
  and refusals; a set, edit or resume that leaves the goal active while
  nothing runs starts one visible turn with Muse Code's cue, replayed but
  not shown as the user's message. No turn of its own after a turn, and no
  completion audit: each would be a model call the user did not ask for.
- **The panel**: the goal strip above the task list (objective, status in
  words, percentage, clamped bar, now and next, Pause or Resume, Edit in
  place with the focus given back to Edit when the field closes, Clear);
  `/goal …` in the prompt with the TUI's grammar, sent as a
  command; `/goal` in the palette and the slash list, which leaves
  `/goal ` ready; a notice for each command done, a warning in words for a
  refusal, the live region reading each change of status; the goal kept in
  the webview's saved state.

## Tests

- `test/unit/mapNotification.test.ts`: the captured goal frames, `null`
  and absent clearing, verbatim status and percentage, a malformed goal.
- `test/unit/sessionRecords.test.ts`: a snapshot's goal, `null` as none,
  inline and items-only histories that cannot say, a goal of another shape.
- `test/unit/MuseCodeHost.test.ts`: each verb on the wire, the captured
  refusals, the snapshot resume bringing the goal and the task list,
  `goalChanged` routed to the session.
- `test/unit/goals.test.ts`: the four tools' results against the captured
  shape (and M43's row reading them), their rules and failures, the user's
  verbs and refusals, the budget, the panel's block, the pinned section
  and the step probe.
- `test/unit/modelApiHost.test.ts`: the tools through a turn with the goal
  pinned into the next request, the goal turn woken by a set (its cue, no
  card, the title), a busy set joining the turn, pause, edit, clear and
  refusals, Stop pausing, the budget, the stored, read, resumed and forked
  goal.
- `test/unit/conversationController.test.ts`: a set starting a session,
  the notices, nothing logged of the objective, the refusals in words, a
  missing objective, a resume and retry after `sessionNotLoaded`, the
  snapshot goal in `historyLoaded`.
- `test/unit/uiState.test.ts`, `snapshot.test.ts`: the goal held and
  announced, a history that cannot say, the goal saved across a reload.
- `test/unit/GoalPanel.test.tsx`, `App.test.tsx`, `goalCommand.test.ts`,
  `paletteRegistry.test.ts`, `protocol.test.ts`, `sessionStore.test.ts`,
  `instructions.test.ts`, `modelApiTools.test.ts`: the strip and its
  controls, `/goal` in the prompt and the menus, the messages, the stored
  shape, the pinned section's place, the tool list.

## Drills

Each drill broke one thing on purpose, ran the tests that guard it (G21:
the accessibility gate over `goal-edit`), and was restored.

| Drill | What was broken                                               | Result                                |
| ----- | ------------------------------------------------------------- | ------------------------------------- |
| G1    | `session/goalChanged` is read as a cleared goal               | 4 failed                              |
| G2    | A resumed snapshot's goal is ignored                          | 3 failed                              |
| G3    | A resume asks for inline history again                        | 3 failed                              |
| G4    | MSP's goal refusals are not recognised                        | 2 failed                              |
| G5    | A bare goal verb carries an objective                         | 1 failed                              |
| G6    | The Model API's goal result loses Muse Code's `session_id`    | 3 failed                              |
| G7    | `create_goal` replaces an unfinished goal                     | 1 failed                              |
| G8    | The goal is not pinned into the instructions                  | 2 failed                              |
| G9    | A goal turn's cue is shown as the user's message              | 1 failed                              |
| G10   | Stop leaves the goal active                                   | 1 failed                              |
| G11   | A token budget never stops the goal                           | 2 failed                              |
| G12   | The goal is not stored with the session                       | 2 failed                              |
| G13   | The panel ignores `goalChanged`                               | 3 failed                              |
| G14   | A history that cannot say drops the same session's goal       | 1 failed                              |
| G15   | The strip offers Pause whatever the status                    | 1 failed                              |
| G16   | The bar is not clamped                                        | 1 failed                              |
| G17   | `/goal` is sent as a message                                  | 2 failed                              |
| G18   | "/goal pause the build" reads as a pause                      | 1 failed                              |
| G19   | A refused resume is not said in words                         | 1 failed                              |
| G20   | The step probe never appears                                  | 1 failed                              |
| G21   | The objective field loses its label (accessibility, 4 themes) | 1 rule violated on 4 elements, exit 1 |
| G22   | The focus is lost when the objective field closes             | 1 failed                              |

A first G21 removed the progress bar's name instead; axe does not require
one on a native `<progress>`, so it passed, and the drill was moved to the
edit field, whose missing label the gate does catch. The bar keeps its
name all the same.

## Seen

Harness scenarios `goal` (set from the prompt, then the agent's progress,
with a task list under the strip) and `goal-edit` (a paused goal with its
objective open for a change) were rendered and looked at, in English, the
pseudo-locale and German; both run in the accessibility gate in the four
themes.

## Translations

The 30 new strings are in all fourteen languages, each table's own
terminology (the goal is "Ziel", "objectif", "目标" as M43's rows already
call it) and typography (French no-break spaces before `:` and `;`, full
width colons in Chinese). `/goal` stays as it is everywhere
(`l10n/untranslated.json`); the placeholder after it is translated
(`/goal <Ziel>`).

## The gate

`npm run quality` exits 0; its tail is in the commit message. The first
run found one clone (the strip's box repeated the task list's CSS; they
now share the rule). One unit test outside M45 (`store.test.tsx`, the
crash screen's Reload) timed out once in a coverage run while other
worktrees ran their gates on the same machine; alone it passes.

## Integration onto M44

On 2026-09-25, M45 was applied to `main` at `bdaede4` in a separate
integration worktree. Shared additions from M44 (image edits and paid image
tools) and M45 (goals) were retained in the changelog, plan, harness list,
tool definitions, session records, constants and Model API tests. The
original agent worktree was left intact.

`npm run quality` exited 0 in the integration worktree. It ran 1,542 tests
(3 skipped), and the accessibility gate checked 260 pages across four
themes: 0 violations, 0 undecided, 0 pages without a result. The
dependency audit, secret scan and SAST found no issues. The earlier
parallel gate attempt in the agent worktree timed out in unrelated tests;
this run was made without the other milestone gates running.

## Review of PR #31

The review found two Model API goal edges after M45 was integrated:

- A completed reply could spend the goal's token budget and still run the
  function calls it returned, then buy another model round. The loop now
  gives each unrun call a cancelled replay output and ends the turn before
  running a tool or sending another automatic request. A later, separate
  user turn can still ask a question.
- A set, edit or resume accepted during a streaming final reply joined the
  running turn but could be left without a model request that saw the new
  goal. The turn now queues an internal goal cue for its next round. The
  cue is replayed for the model, not shown as a new user message.

| Drill | What was broken                                      | Result                       |
| ----- | ---------------------------------------------------- | ---------------------------- |
| G23   | Budget exhaustion no longer stops returned tools     | 1 failed; restore: 70 passed |
| G24   | A busy goal command no longer requests another round | 1 failed; restore: 70 passed |

The second review found two more edges:

- A goal wake queued behind compaction could start after that compaction
  spent the goal's budget. The queued wake is now withdrawn when its goal
  is no longer active; any later queued user turn can still start.
- Pause, resume, edit and clear on a fresh panel created an empty session
  before refusing the command. These verbs now report the absent goal
  without creating a conversation; set still creates one.

| Drill | What was broken                                  | Result                                              |
| ----- | ------------------------------------------------ | --------------------------------------------------- |
| G25   | A stale queued goal wake starts after compaction | 1 failed; restore: 196 passed across affected files |
| G26   | A bare goal verb creates an empty conversation   | 1 failed; restore: 196 passed across affected files |

The queued wake's one new user-facing reason is translated in all fourteen
tables. `npm run quality` on the second review revision exited 0: 1,546
tests passed, 3 skipped; 260 accessibility pages had 0 violations and 0
missing results; secret scanning and SAST had 0 findings. The disabled
ChromeControlMCP extension stayed at 0 native hosts through the gate.

`npm run quality` on the review revision exited 0: 1,544 tests passed, 3
skipped; 260 accessibility pages had 0 violations and 0 missing results;
secret scanning and SAST had 0 findings. During the browser gate, the
persistent Chrome profile launched many ChromeControlMCP native hosts.
That process burst is a local machine risk even though the gate passed;
subsequent local browser gates wait until the extension is disabled or
Chrome is closed. The headless-browser flag and Edge experiments did not
stop it, and neither remains in the project scripts.

The owner disabled the ChromeControlMCP extension. A targeted 8-page
accessibility run then passed with 0 ChromeControlMCP hosts or command
wrappers both before and after; the extension remains disabled during
the final local gate. That gate passed again, with 0 hosts at completion.

## Third review and lifecycle audit

The next PR #31 review found that a streamed response charged whichever
goal was active when it ended, and Stop during compaction did not pause an
active goal. A focused audit also found that rejected `/goal` text vanished
from the composer, an open goal editor could keep an old objective, and an
incomplete compaction could keep a partial summary without charging usage.

- Each Model API request attempt and compaction captures the active goal ID
  it sent. Its usage charges that same goal even if paused before the reply;
  a newly resumed or replaced goal is not charged for an older request.
- Stop pauses the active goal whether it ends a turn or a compaction.
- A compaction accepts only a completed response. An incomplete response's
  reported usage is charged, then its partial summary is refused.
- The goal command has a correlated result. The composer keeps submitted
  text until acceptance and clears only a draft that has not changed since
  submission. The goal editor resets after an external objective change or
  goal removal.

| Drill | What was broken                                                     | Result                                  |
| ----- | ------------------------------------------------------------------- | --------------------------------------- |
| G27   | Usage charged to the goal active at reply end                       | 1 focused test failed                   |
| G28   | Stop during compaction did not pause the goal                       | 1 focused test failed                   |
| G29   | Incomplete compaction skipped usage, or accepted partial text       | 1 focused test failed for each mutation |
| G30   | Rejected command cleared its draft or accepted result used stale ID | Focused UI tests failed                 |
| G31   | Goal editor retained old objective across external changes          | Focused UI tests failed                 |

Each mutation was restored; the five affected test files passed 342 tests.
`npm run quality` on the third review revision exited 0: 1,560 tests passed, 3 skipped;
260 accessibility pages had 0 violations and 0 missing results; audit,
secret scanning and SAST had 0 findings. ChromeControlMCP stayed at 0
native hosts. Hosted CI and PR review remain separate gates.

## Fourth review

The next PR #31 review found an inline edit that closed before host
admission. A refused edit lost the proposed objective. The editor now holds
its text while pending; a matching accepted result closes it only if the
user has not typed a newer objective. A `goalChanged` event can arrive
before that result, so an accepted older edit must also leave newer typing
intact. The host's `goalCommandResult` correlates each attempt.

The review also found the new `session/goalChanged` parser cast was not in
the escape-hatch register. That notification is now parsed under its
literal method, letting TypeScript verify the parsed goal shape without
adding a cast. Existing malformed-goal tests still guard the boundary.

| Drill | What was broken                                    | Result                                                    |
| ----- | -------------------------------------------------- | --------------------------------------------------------- |
| G32   | Inline edit ignores refusal or clears newer typing | Focused App and GoalPanel tests failed                    |
| G33   | Malformed goal frame is not reported as malformed  | 1 focused mapNotification test failed; restore: 34 passed |

The focused App, GoalPanel, conversation controller, protocol and
notification tests passed 304 cases after restoration. The full quality
gate on this revision exited 0: 1,563 tests passed, 3 skipped; 260
accessibility pages had 0 violations and 0 missing results; audit,
secret scanning and SAST had 0 findings. ChromeControlMCP remained at 0
native hosts. Hosted CI and PR review remain separate gates.

## Fifth review

The next PR #31 review found an unchecked `as` cast in the Model API goal
tool-list test. The test now compares the unknown response field against
four named tool objects with Vitest matchers, without asserting its TypeScript
type. All 75 Model API host tests pass; no new gate or wire shape was added.
`npm run quality` on this correction exited 0: 1,563 tests passed, 3
skipped; 260 accessibility pages had 0 violations and 0 missing results;
audit, secret scanning and SAST had 0 findings. ChromeControlMCP remained
at 0 native hosts. Hosted CI and PR review remain separate gates.

## Sixth review

The next PR #31 review found that an old streaming response could complete
or report progress on a goal that the user had replaced or edited while it
streamed. Each Model API request now captures the user goal-command revision
at its start. A `create_goal`, `update_goal` or `report_progress` call from
an older revision fails with a tool output; `get_goal` can still read the
current goal. The queued goal wake then starts a request for the new
objective. Retry attempts each capture the revision of their own request.

| Drill | What was broken                                     | Result                                                                          |
| ----- | --------------------------------------------------- | ------------------------------------------------------------------------------- |
| G34   | An old response completed a replaced or edited goal | 2 focused tests failed with the replacement at 100% complete; restore: 2 passed |

The focused tests, typecheck and localization gate passed after restoration.
`npm run quality` exited 0: 1,565 tests passed, 3 skipped; 260 accessibility
pages had 0 violations and 0 missing results; audit, secret scanning and
SAST had 0 findings. ChromeControlMCP stayed at 0 native hosts. Hosted CI
and PR review remain separate gates.

## Seventh review

The next PR #31 review found two more boundaries. An inline `session/read`
after `view/gap` cannot say whether a goal was cleared or completed; the
panel had kept its old goal. A read-only Muse Code 1.3.0 capture on
2026-09-25 at `C:\muse-live-m45` sent only `session/read` and backward
`view/page` for session `01a0d9f6-168c-7580-bb82-3f43f8dd9606`: **0
model attempts** (`model.attempt.lifecycle` admission count: 0 in
`~/.local/share/muse/local-tracing/bootstrap/cli-2978ce85-….log`). The raw response is
`%TEMP%\muse-goal-gap-probe.json`. `session/read` served `mode: inline`
without a goal; `view/page` served 27 ascending events and `nextCursor:
null`, with six `session/goalChanged` events, the last carrying `goal:
null`. Gap reloads now read backward through durable pages for the last
goal event. No event by the beginning of history means no goal.

An incomplete Model API compaction also charged usage in memory without
saving it. Usage now persists after the goal and cumulative counters are
both updated, even when compaction rejects its partial response.

| Drill | What was broken                                    | Result                                                              |
| ----- | -------------------------------------------------- | ------------------------------------------------------------------- |
| G35   | A gap reload kept an old goal after a missed clear | 1 focused test failed; restore: clear and paged completion passed   |
| G36   | Incomplete compaction usage was not stored         | 2 focused tests failed (with and without a goal); restore: 2 passed |

Focused tests and host typecheck passed after restoration. `npm run quality`
exited 0: 1,569 tests passed, 3 skipped; 260 accessibility pages had 0
violations and 0 missing results; audit, secret scanning and SAST had 0
findings. ChromeControlMCP stayed at 0 native hosts. Hosted CI and PR
review remain separate gates.

## Eighth review and focused audit

The next PR #31 review found that saving usage on every normal Model API
response could persist a `function_call` while its approval or tool output
was still pending. That stored replay would be invalid after a restart.
Turn-start user messages may be saved; a snapshot with any function call
missing its matching output is deferred until the call is answered.
Compaction saves in `finally`, including when its response was incomplete.
The audit found other mid-turn saves from goal tools, user goal commands and
settings changes; the store now defers their unanswered-call snapshots, but
still announces live session-list changes.

The audit also found that a resumed Muse Code session may serve inline
history despite the snapshot preference. It now recovers the goal from the
same durable view pages as gap reload. A live `goalChanged` arriving during
a gap read takes precedence over that older read when the panel reloads.

| Drill | What was broken                                        | Result                                                                                                                                                     |
| ----- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G37   | A snapshot was saved with a pending call and no output | 1 focused test failed (`[false, true, false, false]` snapshots); two additional mid-turn save paths failed, then all 2 tests passed with output IDs paired |
| G38   | An inline resume omitted its existing goal             | 1 focused Muse host test failed; restore: 1 passed                                                                                                         |
| G39   | An older gap read replaced a newer live goal clear     | 1 focused controller test failed; restore: 1 passed                                                                                                        |

The incomplete-compaction persistence tests also passed after the change.
`npm run quality` exited 0: 1,573 tests passed, 3 skipped; 260 accessibility
pages had 0 violations and 0 missing results; audit, secret scanning and
SAST had 0 findings. ChromeControlMCP stayed at 0 native hosts. Hosted CI
and PR review remain separate gates.

## Ninth review

The next PR #31 review found that a set, edit or resume accepted while the
last Model API tool round runs could leave its goal active and idle. The
round cap still fails the old turn, but its pending goal wake now queues a
fresh bounded turn that receives the current objective.

| Drill | What was broken                                         | Result                                                                |
| ----- | ------------------------------------------------------- | --------------------------------------------------------------------- |
| G40   | A busy goal command in round 50 never reached a request | 1 focused test failed at 50 requests instead of 51; restore: 1 passed |

Focused tests passed after restoration. `npm run quality` exited 0: 1,574
tests passed, 3 skipped; 260 accessibility pages had 0 violations and 0
missing results; audit, secret scanning and SAST had 0 findings.
ChromeControlMCP stayed at 0 native hosts. Hosted CI and PR review remain
separate gates.

## Tenth review and focused audit

The audit found three ways accepted work could be lost or duplicated: a
steered message arriving in the final Model API tool round never reached
another request; replacing a goal during compaction left two queued wakes
for the replacement; and Stop during the post-summary token count left an
active goal unpaused. Overflow steering now gets fresh bounded user turns,
queued wakes carry the goal-command revision and withdraw when superseded,
and late Stop pauses the goal while keeping the committed summary.

The next PR #31 review found two panel races. A gap reload now reconciles
an open goal editor with the recovered objective, preserving newer typing
on an acknowledged own edit. A goal acknowledgement received after the
surface switches sessions no longer posts a notice or command result to
the new conversation, or marks it active.

| Drill | What was broken                                         | Result                                                                    |
| ----- | ------------------------------------------------------- | ------------------------------------------------------------------------- |
| G41   | Steering accepted in round 50 was lost                  | 1 focused test failed at 50 requests instead of 51; restore: 1 passed     |
| G42   | Two compaction-time goal wakes ran for the replacement  | 1 focused test failed with no withdrawal and two wakes; restore: 1 passed |
| G43   | Stop after summary commit left the goal active          | 1 focused test failed on active status; restore: 1 passed                 |
| G44   | Gap recovery left the inline editor on an old objective | 1 reducer test failed on stale draft; restore: 1 passed                   |
| G45   | Old goal acknowledgement appeared in the new session    | 1 controller test failed with old notice and result; restore: 1 passed    |

Focused tests and typechecks passed after restoration. Full quality,
hosted CI and PR review remain separate gates.

## Eleventh review audit

The follow-up audit found three more ways a later action could be lost or
misrouted. A `sessionNotLoaded` refusal from an old goal call could resume
the old session over a newer History selection before the goal command's
session guard ran. The retry helper now checks the operated session first.
Stop during post-summary counting now pauses the goal it stopped at that
moment, so a replacement set before counting finishes remains active.
Steering accepted while the old response exhausts the goal budget now runs
as a fresh ordinary user turn; the budget still stops goal tools.

| Drill | What was broken                                     | Result                                                                      |
| ----- | --------------------------------------------------- | --------------------------------------------------------------------------- |
| G46   | A late old-session refusal replaced the new session | 1 controller test failed with two resumes instead of one; restore: 1 passed |
| G47   | Stop paused a replacement goal set afterward        | 1 focused test failed at two requests instead of three; restore: 1 passed   |
| G48   | Budget exhaustion dropped accepted steering         | 1 focused test failed at three requests instead of four; restore: 1 passed  |

Focused tests passed after restoration. `npm run quality` exited 0: 1,582
tests passed, 3 skipped; 260 accessibility pages had 0 violations and 0
missing results; audit, secret scanning and SAST had 0 findings.
ChromeControlMCP stayed at 0 native hosts. Hosted CI and PR review remain
separate gates.

## Twelfth review

The next PR #31 review found that Model API user-goal validation could show
Muse Code's English model-facing overlength error in a translated panel.
The validator now returns a reason code. Goal tools still answer in English;
user commands read a new localized template and format the character limit
with the installed locale. The review also found that suppressing a late
old-session goal acknowledgement left its composer command pending after a
History switch. A different-session `historyLoaded` now clears that
correlation, so the new session's composer can submit normally.
The focused audit also found UTF-16 length double-counted emoji against the
4,000-character limit; validation now counts grapheme clusters, including
joined emoji as one visible character, and stops at the first over-limit
cluster.

| Drill | What was broken                                                  | Result                                                                                                                   |
| ----- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| G49   | A German user saw the English overlength detail                  | 1 focused test failed after an English-only mutation; restored green                                                     |
| G50   | A History switch kept the old pending `/goal` command            | 1 reducer test failed; restored green                                                                                    |
| G51   | UTF-16 length rejected 2,001 emoji under a 4,000-character limit | 1 focused test failed; a joined-emoji drill also failed under code-point counting; restored green with grapheme counting |

Focused tests and the 14-table localization gate passed after restoration.
`npm run quality` exited 0: 1,584 tests passed, 3 skipped; 260
accessibility pages had 0 violations and 0 missing results; audit, secret
scanning and SAST had 0 findings. ChromeControlMCP stayed at 0 native
hosts. Hosted CI and PR review remain separate gates.

## Thirteenth review

The next PR #31 review found that Stop on an ordinary Model API turn could
pause a replacement goal set before the old turn unwound. Stop now pauses
the goal it targeted immediately. A new goal accepted while the aborted
turn is still attached queues a fresh wake, then runs after the old turn
settles. Steering submitted after Stop is refused instead of accepted into
a turn that cannot process it.
A completed response buffered across Stop now receives cancelled outputs
for any returned calls, then ends without queuing earlier steering.

| Drill | What was broken                                           | Result                                                                                          |
| ----- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| G52   | Stop paused a replacement goal while the old turn unwound | 1 focused test failed at two requests instead of three; restored green                          |
| G53   | Steering after Stop was accepted but lost                 | 1 focused test resolved instead of rejecting; restored green                                    |
| G54   | A buffered reply requeued steering after Stop             | 1 focused test started a third turn; restored green with cancelled call outputs and no new turn |

Focused tests passed after restoration. `npm run quality` exited 0: 1,587
tests passed, 3 skipped; 260 accessibility pages had 0 violations and 0
missing results; audit, secret scanning and SAST had 0 findings.
ChromeControlMCP stayed at 0 native hosts. Hosted CI and PR review remain
separate gates.
